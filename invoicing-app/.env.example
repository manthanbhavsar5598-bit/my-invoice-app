# URL of the deployed backend API, including the /api/v1 prefix.
# Set this in Vercel's Project Settings -> Environment Variables for the
# frontend project (not in this file, which is just documentation).
VITE_API_URL=https://your-backend-project.vercel.app/api/v1
